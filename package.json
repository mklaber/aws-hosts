{
  "name": "aws-hosts",
  "version": "0.0.1",
  "description": "Outputs a hosts file entry from an AWS url. You can then paste it in to your hosts file.",
  "main": "cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Klaber <klaber@gmail.com> (http://github.com/mklaber/)",
  "license": "MIT",
  "bin": {
    "aws-hosts": "./cli.js"
  },
  "dependencies": {
    "commander": "^2.15.1"
  }
}
